apiVersion: batch/v1
kind: CronJob
metadata:
  name: guild-tracker-fetcher-hourly
  namespace: guild-tracker
spec:
  # :05 past the hour - clear of the daily job below, which lands on :00.
  schedule: "5 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      activeDeadlineSeconds: 900
      template:
        spec:
          restartPolicy: OnFailure
          imagePullSecrets:
            - name: ghcr-pull
          containers:
            - name: fetcher
              image: ghcr.io/itslukv/guild-tracker:latest
              command: ["/app/fetcher", "--mode", "hourly"]
              envFrom:
                - secretRef:
                    name: guild-tracker-env
              resources:
                requests:
                  cpu: 100m
                  memory: 64Mi
                limits:
                  cpu: "1"
                  memory: 256Mi